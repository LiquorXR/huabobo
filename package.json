{
  "$schema": "https://json.schemastore.org/package",
  "name": "flower-bun-system",
  "version": "1.0.0",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "dev": "wrangler pages dev ./public --ip 0.0.0.0 --local-protocol https",
    "deploy": "wrangler pages deploy ./public"
  }
}
